type JiraChannelPrefs @rootType {
    channel: String @compositeId
    issueCreated: Boolean
    issueDeleted: Boolean
    issueComment: Boolean
    issueStatus: Boolean
    issueState: Boolean
    bug: Boolean,
    task: Boolean,
    epic: Boolean,
    story: Boolean,
    subtask: Boolean,
}
