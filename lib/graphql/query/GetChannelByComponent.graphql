query GetChannelByComponent ($componentId: String!, $projectId: String!) {
  JiraComponentMap (componentId: [$componentId], projectId: [$projectId]) {
    channel
    projectId
    componentId
    active
  }
}